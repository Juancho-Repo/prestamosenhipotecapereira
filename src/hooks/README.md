# 📁 src/hooks/

Hooks reutilizables de JavaScript (para cuando el proyecto migre a React/Vue).

## Ejemplos de uso futuro

```
src/hooks/
├── useIntersectionObserver.js  # Observer para animaciones scroll
├── useScrollPosition.js        # Posición del scroll actual
└── useMediaQuery.js            # Breakpoints responsivos
```
