# 📁 src/layouts/

Plantillas de layout reutilizables (útil si tienes múltiples páginas).

## Ejemplos de uso futuro

```
src/layouts/
├── MainLayout.html    # Layout con nav + footer (página principal)
└── LegalLayout.html   # Layout simple para páginas de términos/privacidad
```
